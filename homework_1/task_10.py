from decimal import Decimal
a = 100.23459879
b = 2.09870983745
print(f"Precise result of multiplying is {Decimal(str(a)) * Decimal(str(b))}")

# NOT_FOR_REWORK: good code but try to split code next time in next pattern:
# imports ....

# variables declaration

# operations with functions like print()

# return value if it is some function / method / generator / coroutine
